# 등속 운동과 그래프 실험실 — movement

## ① 앱의 목적과 대상 단원

- **목적** : 중학교 3학년 과학 「Ⅲ. 운동과 에너지」의 **등속 운동과 그래프**를
  실험실 · 미션 · 학습지로 익히게 한다.
- **대상** : 중학교 3학년 · 과학 (2022 개정)
- **관련 성취기준** : `[9과19-01]` 계열 — 등속 운동하는 물체의 **시간-이동 거리 · 시간-속력** 관계.
  (선생님의 2026 평가계획 「Ⅲ. 운동과 에너지」 성취수준 기술에서 확인함)

> ⚠ **성취기준 원문을 지어내 적지 않았다.** 별책 교육과정 PDF 를 이 PC 에서 찾지 못해
> 코드 번호만 옮기고 학습 목표는 **평가계획의 표현**을 따랐다. 원문 대조는 아직 안 했다.

### 이 앱이 채운 자리

`EnergyKeeper/CLAUDE.md` 의 다음 할 일에 이렇게 적혀 있었다 —
> v-t 그래프(시간-속력)를 자유 낙하 장면에 추가 → `[9과19-01]` 까지 이어진다.

EnergyKeeper 는 **자유 낙하 · 역학적 에너지**(`9과19-02·03·04`)를 다루고,
이 앱은 그 **앞**, **등속 운동과 두 그래프**(`9과19-01`)를 다룬다. 두 앱은 겹치지 않는다.

### 출처

```
Desktop\수업자료\수업_과학3(김선은샘)\수업_과학3_대단원3(운동과에너지)\과학3_대단원3_PPT.pptx
  → 슬라이드 1~7 : 시간-거리 · 시간-속력 그래프 읽기 (A/B 비교, 속력 4 m/s × 5 s 등)
Desktop\김미란 교수학습-평가계획서\2026. 거제중 3학년 과학과 평가계획.hwpx
  → 성취수준 기술 : "등속 운동의 의미를 알고 … 시간-거리 그래프에서 속력을 구할 수 있다"
```

> ⚠ 같은 폴더의 `과학3_대단원3_학습지.hwp` 는 **읽지 못했다**(hwp 바이너리 · 이 PC 에 파서 없음).
> 학습지 문항은 PPT 의 그래프 문제 **유형**을 따라 새로 썼다. 선생님이 원본 학습지와 비교해 주면 좋다.

### 왜 이렇게 만들었나 (설계 의도)

**시각 t 하나가 세 장면을 동시에 움직인다.** 트랙 위의 수레 · 1초 간격 잔상 · 두 그래프의 점.
학생이 "트랙에서 본 것"과 "그래프에서 본 것"이 **같은 것**임을 스스로 잇게 하는 것이 전부다.

1. **잔상 간격 = 그 1초 동안 이동한 거리.** 등속이면 일정, 아니면 변한다.
2. **그래프가 같은 함수에서 나온다.** `Movement.dist / speed` 하나로 트랙 · 잔상 · 두 그래프 ·
   학습지 그림 · 표 정답이 모두 결정된다. 서로 어긋날 수 없다.
3. **「같은 곳에 도착해도 운동은 다르다」.** 등속(5 m/s) · 점점 빨라짐 · 점점 느려짐이
   모두 **10초 뒤 50 m**. 5초 때는 37.5 / 25 / 12.5 m 로 갈린다. 이것이 마지막 장면의 결론.

> ⚠ **직선 운동만 다룬다. '가속도'라는 말을 쓰지 않는다**(중3 범위 밖).
> "점점 빨라진다 / 점점 느려진다"라고만 말한다. 빨라지는 정도(1 m/s²)는 화면에 내놓지 않는다.
> ⚠ 시간-속력 그래프 아래 **넓이 = 이동 거리**는 화면에서 말하지 않는다. 학습지 13번에서만
> "가로 × 세로"로 곱하게 했다(PPT 슬라이드 6의 4 m/s × 5 s 와 같은 문제).

### 자료 값 (학습지에 없어 이 앱이 정한 것)
관찰 시간 0~10 s · 트랙 0~100 m · 속력 0.5~10 m/s(0.5 단위) ·
점점 빨라짐 `d = ½t²`(v = t) · 점점 느려짐 `d = 10t − ½t²`(v = 10 − t) · 비교용 등속 5 m/s.

### 🌐 배포 — GitHub Pages (2026-09-19 부터)

**학생은 GitHub 를 만지지 않는다. 주소 하나만 받는다.**

| 무엇 | 값 |
|---|---|
| 저장소 | https://github.com/science-creator/movement (**공개**) |
| 실험실 | https://science-creator.github.io/movement/ |
| 학습지 | https://science-creator.github.io/movement/worksheet.html |
| 선생님용 | https://science-creator.github.io/movement/teacher.html |
| 정답지 | https://science-creator.github.io/movement/worksheet.html?key |

> ⚠ 저장소가 공개라 **정답지 주소를 아는 사람은 누구나 정답을 본다.** 학생에게는 알리지 않는다(다른 앱도 같다).
> ⚠ 학습지는 선생님 검토 전이다. 학생에게 주소를 알리는 것은 검토 뒤에.

빌드가 없고 외부 의존성이 0개라 저장소의 파일이 그대로 웹 주소가 된다(`server.py` 는 로컬 확인용).
Pages 설정은 Source `Deploy from a branch` · `main` · `/ (root)`.

#### 고친 것을 반영하는 방법

```bash
git add .
git commit -m "무엇을 고쳤는지 한 줄"
git push
```

## ② 기능 목록

| 파일 | 설명 |
|---|---|
| `index.html` | 실험실. 장면 3개 + 미션 6개 |
| `worksheet.html` | 학습지 **20문항**(자동 채점 16) · `?key` 로 정답지 |
| `teacher.html` | 선생님용 제출물 정리 (파일 이름만 읽음) |
| `js/movement.js` | 계산 엔진 (`dist` · `speed` · `slope` · `gaps`) |
| `js/lab.js` | 실험실 화면 (`requestAnimationFrame` 은 ▶ 재생 중에만 돈다) |
| `js/pdf.js` · `css/app.css` | **다른 앱과 같은 파일**(friction 과 바이트 단위로 동일) |
| `server.py` | 로컬 서버 (**9700번대**) |
| `로컬서버_실행.bat` | CP949 + CRLF (chcp 넣지 않음) |

장면 — 🚗 달려 보기 · 🏁 누가 더 빠를까 · 🔀 등속이 아닌 운동 · 🏆 미션 6개.
학습지 5단계 — 등속 운동이란 · 시간-이동 거리 · 시간-속력 · 등속이 아닌 운동 · 정리.

## ③ 검증 (2026-09-19)

임시 정적 서버(PowerShell `HttpListener`)로 브라우저에서 돌려 확인했다.

### 그림이 말하는 수 — 그려진 픽셀을 엔진 값과 대조
캔버스의 `arc` · `fillText` 를 가로채 좌표를 모았다.

| 검증 내용 | 결과 |
|---|---|
| **잔상 점의 x** = 눈금 0점 + d(s) × px/m | 4장면(속력 3 · 7.5 · A6/B2.5 · 3운동) × 시각 5개 — **어긋남 0** |
| **잔상 사이 px ÷ (px/m)** = 그 1초 동안의 이동 거리 | 일치 |
| **그래프의 점** (두 그래프 · 모든 계열) 위치 = 눈금×값 | 일치 (검사 **585 + 410 항목** 통과) |
| 세 운동이 10 s 에 | 50 m / 50 m / 50 m · 속력 5 / 10 / 0 |
| 5 s 에 | 25 / 12.5 / 37.5 m |
| 잔상 간격 | 빨라짐 0.5·1.5·2.5·3.5·4.5 · 느려짐 9.5·8.5·… · 등속 4·4·4… |
| 기울기 = 속력 | 등속에서 어느 구간을 재도 6 = 6 |
| **학습지 그림** (잔상 · A/B 그래프 · 속력 그래프) | 좌표를 값으로 되돌려 보니 0·4·…·20 m / A 20 m · B 10 m / 4 m/s × 5 s — **모두 정답과 일치** |
| 글자 넘침 (무대 + 그래프 두 장) | 375 px 폭에서 5장면 × 21시각 — **0건** |

### 미션 · 학습지

| 검증 내용 | 결과 |
|---|---|
| 미션 6개 | 성공 경로로 모두 `won`. **시작 상태는 6개 모두 목표 미달성** |
| 잘못된 상태로는 안 깨짐 | 미션 2(v=3) · 3(A=B) · 4(속력 하나) → `ready` 유지 |
| 학습지 채점 | 만점 **16/16** · 전부 첫 선택지만 찍으면 **2점**(기대 2점과 일치) |
| 정답 위치 분포 | 1번 2 · 2번 5 · 3번 3 (선택 10문항) |
| 표 정답 | 엔진 값과 일치 |
| 정답지(`?key`) | 20문항 · 빈 정답 **0** |
| PDF | 5쪽 · 파일 이름 `등속운동학습지_3-4-12_…pdf` → `teacher.html` 이 판독 |
| 재생 | ▶ 후 1.5 s 에 t = 1.46 · 멈춤 유지 · ⏭ 1초 뒤로 |
| 콘솔 오류 | 없음 |
| `file://` 전제 | 모듈 · fetch · CDN · localStorage **0건** |

### 검증 중에 고친 것
1. 하단 설명 문구가 안내 알약과 겹쳤다. → 위로 올렸다.
2. 좁은 폭(≤720 px)에서 **세 줄 레인이 빽빽해** 마지막 줄의 초 표시가 문구와 겹쳤다.
   → 좁은 화면의 무대 비율을 1.05 → 0.8 로. 자리가 좁으면 수레 위치 값 표시는 생략(값은 계기판에 있다).
3. 그래프 범례가 위쪽 값 표시와 겹쳤다. → 범례 전용 띠를 위에 만들었다.
4. **최댓값(10 m/s, 100 m)의 값 표시가 그래프 위로 잘렸다.** → 점 아래로 그린다. (수치 검사로 찾음)
5. 정답이 1번 선택지에 **8/14** 로 쏠려 있었다. → 4 문항의 선택지 순서를 바꿨다.

### 인쇄 쪽수 (2026-09-19) — Edge 를 화면 없이 돌려 PDF 로 인쇄해 쪽수를 셈
`msedge.exe --headless=new --no-pdf-header-footer --print-to-pdf=출력.pdf file:///…/worksheet.html`

| 고친 단계 | 학습지 쪽수 |
|---|---|
| 처음 (그림 6장 · 문항 여백 그대로) | **5쪽** |
| 그래프 그림을 묶음마다 한 번만(6장 → 3장) | 4쪽 |
| 인쇄용으로 문항 여백을 줄임 (`.qbox` · `.qno` · `.fig`) | **3쪽** (마지막 쪽은 3분의 1 남짓) |
| **27문항 → 20문항으로 축소** (선생님 요청, 같은 내용을 다시 묻는 7문항 삭제) | **3쪽** (마지막 쪽 8줄) · 정답지 1쪽 ||
| 정답지 `?key` | 1쪽 |

⚠ 쪽 안의 실제 모양은 **눈으로 못 봤다**(PDF 를 그림으로 바꾸는 도구가 이 PC 에 없음). 쪽수만 셌다.
### 20문항으로 줄인 기록 (2026-09-19)선생님이 **학습지는 20문항 이하**로 하라고 하셔서 27 → 20 으로 줄였다. 단원의 핵심 개념은 그대로 두고 **같은 것을 다시 묻는 문항**을 뺐다.빠진 것 : 1단계 「그림의 수레는 1초에 몇 m」 · 2단계 「B 의 속력」·O/X 「가파를수록 빠르다」 · 3단계 「속력이 큰 물체의 수평선 위치」 ·4단계 「빨라질 때 잔상 간격」·「느려질 때 그래프 모양」 · 5단계 「등속 운동에 가까운 생활 예」.단계별 문항 수 5 · 5 · 4 · 5 · 1. 그림 문항이 가리키는 「위 그림」이 앞뒤로 맞는지 확인했다.

### 아직 못 한 것
**실제 프린터 출력**(아래 「인쇄 쪽수」는 Edge 의 PDF 인쇄로만 확인) ·
PDF **파일을 열어 눈으로 확인** · `file://` 더블클릭 · 태블릿 터치 · `server.py` 실행(파이썬 없음) ·
학습지 화면의 **눈으로 본 확인**(그림은 수치로만 확인함).

### 다음 할 일
- [ ] 선생님이 학습지를 원본 `과학3_대단원3_학습지.hwp` 와 견주어 검토
- [ ] 종이로 한 번 뽑아 글자 크기·그림 크기 확인 (쪽 수는 아래처럼 확인함)
- [ ] 이어서 : 자유 낙하(속력이 일정하게 커짐)를 이 앱의 **점점 빨라짐**과 잇는 장면 —
      `EnergyKeeper` 의 「다음 할 일」과 만난다
- [x] GitHub 공개 + Pages 배포 (2026-09-19) — 세 주소가 열리고 배포본에서 동작하는 것까지 확인함

## 코드 메모
- **모든 그림은 `Movement.dist / speed` 에서 나온다.** 그림 안에 숫자를 손으로 적지 말 것.
- 미션은 `MISSIONS` 배열에 항목만 넣으면 된다. `setup` 은 시작 상태(목표가 참이면 안 됨),
  `allow` 는 조작판에서 열어 줄 슬라이더(`v` `vA` `vB` `t`).
- 캔버스 좌표 검사는 `arc` 반지름으로 구분한다: 잔상 점 r=4 · 그래프 점 r=5.5 · 바퀴 r=5.
- `MvLab._test` (`set` `scene` `pick` `answer` `goals` `state` `series` `geometry` `draw`) ·
  `MVSheet` 는 검사용 손잡이.
- 브라우저 도구가 `file://` 를 정적 스냅샷으로만 열어 스크립트가 안 돈다. 이 PC 에는 파이썬·node 가 없으니
  검증할 때는 PowerShell `System.Net.HttpListener` 로 임시 서버를 띄우면 된다(끝나면 끌 것).
